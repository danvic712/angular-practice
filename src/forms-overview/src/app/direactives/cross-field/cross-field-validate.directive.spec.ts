import { CrossFieldValidateDirective } from './cross-field-validate.directive';

describe('CrossFieldValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new CrossFieldValidateDirective();
    expect(directive).toBeTruthy();
  });
});
