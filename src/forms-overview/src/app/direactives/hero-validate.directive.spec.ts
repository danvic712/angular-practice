import { HeroValidateDirective } from './hero-validate.directive';

describe('HeroValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroValidateDirective();
    expect(directive).toBeTruthy();
  });
});
